# Plateside, the link

The static app, built from the private repository by `labtrack web build` and published by `tools/publish.py`. Code only: nothing anyone types into the app ever reaches this repository or the host that serves it. Open https://justintroy-stack.github.io/plate-app/ in Safari and add it to the home screen.
